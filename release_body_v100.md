# ShyBoard v1.0.0

正式版。此前 workbench 系列的自动更新机制存在缺陷，仅作为测试版；本次改名 **ShyBoard** 并完成更新机制重构，作为真正可迭代的正式版重新开始。

## ✨ 本次更新（正式版首发）

- **品牌改名**：Workbench → **ShyBoard**（害羞的小看板），exe/进程/UI/文档/安装包全链路统一
- **自动更新机制重构**（对标 Clash Verge Rev）：下载与安装解耦，下载完成写 pending 缓存，重启时由独立 PowerShell helper 替换 exe 并自动重启，全程不卡死、不丢数据
- **安装/卸载系统**：写入 Windows 注册表，可在 设置→应用 中查看与卸载；开始菜单可搜索
- **修复**：DETACHED_PROCESS 导致 PowerShell 静默退出的根因；check 缓存误报；更新弹窗重复按钮；安装器路径混合/黑窗闪烁/重复安装等
- **新增安装器**：ShyBoardInstaller-v1.0.0.exe 选择目录安装，自动建桌面快捷方式 + 开始菜单入口 + 注册表卸载入口

## 📥 下载地址

### Windows 10/11（64 位）

- **安装版（推荐）**：ShyBoardInstaller-v1.0.0.exe —— 双击运行，选择安装目录；可在 设置→应用 中管理/卸载
- **绿色版**：ShyBoard-v1.0.0.zip —— 解压即用，双击 ShyBoard.exe 即可

> ⚠️ **首次运行提示**：程序未购买代码签名证书，Windows SmartScreen 可能提示"已保护你的电脑"。这是无签名程序的正常现象（非病毒）。点「更多信息」→「仍要运行」即可；绿色版解压后通常不会触发。

> 需 Edge WebView2 运行时（Win11 内置；Win10 一般已随 Edge 安装，若提示缺少可到微软官网下载一次）

### 安装/更新/卸载

- **安装**：下载安装版 → 双击 → 选目录 → 完成
- **更新**：应用内右上角 ⬆ 按钮，自动从 GitHub Release 检查并更新
- **卸载**：设置 → 应用 → ShyBoard → 卸载（自动删文件/快捷方式/注册表）；或直接删除安装目录

## 🐛 常见问题

- 更新后提示"还有新版本"：检查更新的缓存最长 5 分钟，稍等再点；若仍异常可关掉重开
- 更新过程卡住：看 `data\updates\update.log` 日志（每次更新都会记录）
- 数据位置：安装目录下 `data\workbench.db`（任务/便签/设置全在此），删除文件夹即完全卸载

Created at 2026-08-07.
