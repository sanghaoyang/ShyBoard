# 工作台 Workbench · 使用手册

本地个人工作台：任务管理 + 天气 + 便签 + 快捷方式，支持 AI Agent 接入。
本文按功能模块说明用法；接口文档见 README.md。

---

## 〇、启动方式

三种方式任选，都不会依赖终端窗口：

- **桌面快捷方式「工作台」**（推荐）：双击即开，无任何命令行窗口
- **双击 `start.bat`**：cmd 窗口一闪即关，工作台后台运行
- **终端运行 `python app.py`**：命令立即返回，可继续用终端；关终端不影响工作台
  （应用会自动脱离终端转为后台进程）

关闭工作台 = 关闭工作台窗口本身。数据实时落盘，关错也不丢。

---

## 一、任务管理

### 创建任务
- 顶部输入框输入标题，回车或点「添加」即创建（默认：待办 / 中优先级）
- 点「⋯」展开更多选项：优先级（高/中/低）、截止日期、标签（逗号分隔）、描述
- 创建后输入框自动清空，可连续添加

### 三泳道流转
界面分三列：待办 / 进行中 / 已完成

| 操作 | 方式 |
|------|------|
| 开始做 | 待办卡片点「开始 →」，进入进行中 |
| 完成 | 进行中卡片点左侧圆形勾选框，进入已完成（自动记录完成时间） |
| 恢复 | 已完成卡片再点勾选框，回到待办 |
| 编辑 | 卡片点「✎ 编辑」：改标题/描述/状态/优先级/截止日期/标签 |
| 删除 | 卡片点「✕ 删除」（有确认提示） |

### 任务日志（时间线）
- **点任务卡片的任意区域**（标题/描述/标签，按钮和勾选框除外）→ 打开任务详情
- 详情页显示：完整信息（状态/优先级/标签/截止日期/来源）+ 时间线
- 时间线记录：创建时间、每次进度更新（待办→进行中→已完成）、每次编辑
- **鼠标悬停编辑记录**可看变更细节，例如：`描述：旧内容 → 新内容`、`优先级：中优先级 → 高优先级`

### 排序规则
卡片按优先级排序（高 > 中 > 低），同优先级按截止日期就近在前。

---

## 二、天气

### 查看
- 顶栏右侧实时显示：图标 + 温度 + 城市 · 天气
- 鼠标悬停：湿度 / 风 / AQI / 日出日落
- **点击顶栏天气** → 弹出预报详情：
  - 当前天气大卡片（温度 / 天气 / 湿度 / 风 / AQI / 日出日落）
  - 未来 7 天预报（星期 / 日期 / 图标 / 天气 / 高温低温，今天高亮）
  - 「修改城市」按钮进入换城市

### 切换城市
1. 点顶栏天气 → 预报弹窗 → 「修改城市」
2. 输入城市名（仅支持国内 447 城，如「杭州」「深圳」）
3. 点建议结果（显示「城市 · 省份」）即切换，天气自动刷新

> 天气数据源：中国天气网（国内城市准确）；断网时显示「天气不可用」。

---

## 三、便签

- 侧栏「便签」区域 → 点「＋」新建，输入内容保存
- 悬停便签显示「✕」可删除
- 便签按时间倒序排列，显示创建时间

---

## 四、快捷方式

- 侧栏「快捷方式」区域 → 点「＋」添加：名称 / 网址 / 图标（emoji 可选）
- 网址可以不写 https://，自动补全
- 点击条目在新标签打开；悬停显示「✕」可删除

---

## 五、任务统计

侧栏顶部实时统计：待办数 / 进行中数 / 已完成数 / 今日完成数
任何任务操作后自动刷新。

---

## 六、番茄钟

侧栏番茄钟面板，专注工作法计时器：

- 点击「开始」→ 25 分钟专注倒计时（红色显示）
- 专注中可「暂停 / 继续」，也可「跳过」直接进休息
- 专注结束自动提示音 + 通知，进入 5 分钟休息（绿色显示）
- 休息结束回到就绪，可开始下一个番茄
- 每完成一个专注自动计数（🍅 今日 N），跨天自动归零，数据存本地
- Agent 可查询 / 上报：GET /api/pomodoro、POST /api/pomodoro/complete

---

## 七、AI Agent 接入

Hermes 等 Agent 可通过本地 API 直接操作工作台（只监听 127.0.0.1，安全）：

| 场景 | 命令示例 |
|------|---------|
| 帮我建个任务 | POST /api/tasks 传 {"title":"...","source":"agent"} |
| 完成任务 | PATCH /api/tasks/{id} 传 {"status":"done"} |
| 查询任务 | GET /api/tasks |

- Agent 创建的任务自动带 🤖 标记，一眼可辨
- 状态流转也会记录到时间线（和手动操作一致）
- **长期任务自动跟踪协议**（CLI 下达 → 自动创建 → 更新进展 → 改状态）：见 [WORKFLOW.md](WORKFLOW.md)，任何机器/任何 agent 可复现
- 完整接口表见 README.md「Agent 接入」一节

---

## 八、数据与隐私

- 全部数据存在本地 `data/workbench.db`（SQLite），无账号、无云同步
- 任务、便签、快捷方式、设置、操作日志都在这个文件里
- 换机器：把整个 workbench 文件夹拷走即可迁移

---

## 九、分享给朋友

1. 双击 `build.bat` 打包（产物在 `dist\\Workbench\\`，自动带上 `update.ps1`）
2. 把整个 `dist\\Workbench` 文件夹发给朋友（或发 GitHub Release zip）
3. 朋友双击 `Workbench.exe` 即用，数据存各自本地互不影响
4. 要求：Windows 10/11（需 Edge WebView2 运行时，Win11 内置）

---

## 九·五、自动更新

- 右上角 ⬆ 按钮：检查 GitHub Release 是否有新版本；有则提示「下载并更新」
- 下载完成后自动应用并重启，无需手动操作（下载与安装解耦，不会卡死）
- 更新只替换 exe 和运行库，**不动 data\\ 目录**（任务/便签/设置都在）
- 发布新版本流程见 README「打包分发」节

---

## 十、常见问题

| 问题 | 说明 |
|------|------|
| 双击没反应 / 端口被占 | 服务默认端口 17890，被占自动顺延，端口见 data/port.txt |
| 开了两个窗口 | 正常，是连同一个服务，数据一致 |
| 天气不准 | 国内城市用的中国天气网数据；如仍有出入可先切换城市试试 |
| 搜不到国外城市 | 设计如此（仅支持国内 447 城） |
| 更新后提示"还有新版本" | 检查更新的缓存最长 5 分钟，稍等再点；若仍异常可关掉重开 |
| 更新过程卡住 / 没反应 | 看 `data\\updates\\update.log` 日志（每次更新都会记录） |
| 想重置数据 | 关掉应用，删除 data/workbench.db，重启即全新 |

